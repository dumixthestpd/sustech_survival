---
name: Feature request
about: Suggest a new feature or improvement
title: "[FEAT] "
labels: enhancement
assignees: ""
---

## Subsystem

Which subsystem does this affect?
(`sso` / `tis` / `tis.classroom` / `selectcourse` / `bb` / `lib` / `lib.booking` /
`pms` / `papers` / `nces` / `transit` / `faculty` / `ws` / `webui` / `context` /
`calendar` / `cli`)

## Problem

What problem are you trying to solve?

## Proposed solution

How do you imagine this working? Code snippets, CLI examples, or API
sketches welcome:

```python
# sketch
```

```bash
# CLI sketch
sustech <sub> <command> ...
```

## Alternatives considered

What other approaches did you think about, and why is this one better?